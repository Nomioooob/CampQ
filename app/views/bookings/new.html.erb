<div class="p-5 d-flex justify-content-center align-items-center container">
  <div class="row">
    <div class="d-flex justify-content-between align-items-center">
      <div class="my-5">
      <h1>New reservation</h1>
      <%= render 'form', product: @product, booking: @booking %>
      </div>
      <div>
        <%= cl_image_tag @product.photos[0].key, width: 500, class: "mx-5" %>
      </div>
    </div>
  </div>
</div>
